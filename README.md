# Report-10-2


---

# 📋 گزارش پروژه: انیمیشن چهره‌های احساسی روی ماتریس LED 8×8 با MaxMatrix

## ۱. مقدمه  
در این پروژه, یک **ماتریس LED 8×8** (با کنترلر MAX7219) به آردوینو متصل شده و با استفاده از کتابخانه `MaxMatrix.h`, چهار الگوی باینری برای چهره‌های مختلف تعریف شده‌اند. سپس با فراخوانی `m.writeSprite()`, هر چهره به صورت کامل روی ماتریس نمایش داده می‌شود .



---
)

---

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- ماتریس LED 8×8 با کنترلر **MAX7219**  
- برد برد و سیم‌های جامپر  
- منبع تغذیه (5V از آردوینو کافی است)
---

## ۵. کد برنامه


```cpp
#include <MaxMatrix.h>

int DIN = 2;
int CLK = 4;
int CS = 3;
int maxInUse = 1;

MaxMatrix m(DIN, CS, CLK, maxInUse);

byte poker[] = {8, 8,
                0xff,
                0x81,
                0xa5,
                0xa1,
                0xa1,
                0xa5,
                0x81,
                0xff
               };
byte smile[] = {8, 8,
                0xff,
                0x81,
                0xb5,
                0xa1,
                0xa1,
                0xb5,
                0x81,
                0xff
               };
byte sad[] = {8, 8,
              0xff,
              0x81,
              0xb5,
              0x91,
              0x91,
              0xb5,
              0x81,
              0xff
             };
byte kiss[] = {8, 8,
               0xff,
               0x81,
               0xb5,
               0xb1,
               0xb1,
               0xb5,
               0x81,
               0xff
              };
void setup() {
  m.init();
  m.setIntensity(5);
}
void loop() {
  m.writeSprite(0, 0, smile);
  delay(1000);
  m.clear();
  m.writeSprite(0, 0, poker);
  delay(1000);
  m.clear();
  m.writeSprite(0, 0, sad);
  delay(1000);
  m.clear();
  m.writeSprite(0, 0, kiss);
  delay(1000);
  for (int i = 0; i < 8; i++) {
    m.shiftLeft(false, false);
    delay(300);
  }
  m.clear();
}


```.

---

## ۶. نحوه‌ی کارکرد (مرحله‌به‌مرحله)
1. **لبخند**: الگوی `smile` روی ماتریس نمایش داده می‌شود → 1 ثانیه  
2. **پاک کردن** → الگوی `poker` نمایش داده می‌شود → 1 ثانیه  
3. **پاک کردن** → الگوی `sad` نمایش داده می‌شود → 1 ثانیه  
4. **پاک کردن** → الگوی `kiss` نمایش داده می‌شود → 1 ثانیه  
5. **انیمیشن اسلاید**: هر چهره 8 بار به سمت چپ اسلاید می‌شود (هر بار 300ms)  
6. پاک کردن و تکرار از ابتدا



---

## ۷. نتایج  
- ماتریس LED به درستی چهار چهره را نمایش می‌دهد.  
- انیمیشن اسلاید به سمت چپ به درستی انجام می‌شود.  
- کد کارآمد است و مصرف حافظه پایینی دارد (در تصویر: ~2144 bytes).

---
.


---
